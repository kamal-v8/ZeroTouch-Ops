apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: coffee-app
spec:
  selector:
    app: coffee-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
